# AI_CURRENT_STATE — Текущий контекст

> Читать в начале каждого диалога. Обновлять при каждом значимом продвижении.
> Обновлено: 2026-07-11.

## ⭐ RESUME HERE (2026-07-18) — ПРОД ЗАПУЩЕН 🚀
Боевой сервер поднят: **https://bi.186.246.7.236.sslip.io** (Ubuntu 22.04, 8ГБ/4CPU, IP 186.246.7.236, каталог
`/opt/livs-bi`). Стек тот же + Caddy (авто-HTTPS Let's Encrypt) через прод-оверлей. Домена нет → используем бесплатный
`sslip.io` (DOMAIN=bi.186.246.7.236.sslip.io в .env); позже сменить на реальный .ru = одна строка + A-запись.
- Metabase остался на **H2** (оверлей НЕ переключает на Postgres) → перенеслись готовые 15 дашбордов/юзеры/локализация
  из `metabase-data/` снапшота. Данные восстановлены из `backup/livs_bi.dump` (pg_restore --clean): 345575 продаж, 100
  клиентов, 5848 план-месяц — совпало с локальным.
- **Docker Hub 429 (Too Many Requests)** при pull на РФ-IP → лечили зеркалом реестра: `/etc/docker/daemon.json`
  `registry-mirrors: [dockerhub.timeweb.cloud, daocloud.io, huecker.io]` + `systemctl restart docker`. Помогло.
- Логины — как локально (H2): админ = email, view-only = viewer@livs.local (пароли в CREDENTIALS.local.md).
- ⚠️ ОСТАЛОСЬ на проде: (1) включить авто-бэкапы (scripts/backup.sh + cron 3:30), (2) сменить пароль БД (сейчас change_me,
  но Postgres наружу закрыт — только localhost+файрвол), (3) периодически копировать backups/ офлайн (там ПДн).
- Деплой через полный/деплой-снапшот из `d:\ToLivs\snapshots\`. Прод-развёртывание описано в DEPLOY.md.

## ⭐ RESUME HERE (2026-07-18) — помесячный слой план-факта
Дашборд **id=133 «План-факт помесячно»** (коллекция LIVS BI). Источник — **Sales Retail «SI-SO-ST»** (помесячный
план/факт/форкаст по 47 сетям, показатели Sell In/Out шт+руб, Stock). План стоит по **Sell-In** (руб+шт), помесячно.
- Таблица `core.fact_plan_month` (грейн: Канал·Сеть·КАМ·Показатель·Год·Тип·Месяц; тип=план|факт|форкаст) + вьюха
  `marts.v_plan_fact_month` — в `db/9A_plan_month.sql` (self-contained, поднимается в init). Загрузчик `app/load_plan_month.py`
  (метка периода col6 → (год,тип) через PERIOD_MAP; дублей нет). 5848 значений.
- ⭐ КЛЮЧЕВОЕ: колонка **`expected = COALESCE(факт, форкаст)`** = «живое» ожидание (факт по закрытым месяцам ~7,
  прогноз по будущим ~5). Выполнение% и KPI считаем по expected, НЕ по голому факту (иначе занижение: факт 81М vs план 194М;
  с expected → 167М = 85.7%). Это модель руководителя «закрытые=факт, будущие=прогноз».
- Дашборд `app/build_plan_month_dashboard.py`: фильтры Клиент/Показатель(деф. Sell In руб)/Год(деф.2026); 3 KPI +
  график План vs Факт/прогноз по месяцам + таблица по месяцам + обзор по клиентам. Годовой план 194.7М бьётся с
  квартальным id=70 (187.7М). Дашбордов теперь **15**.

## ⭐ RESUME HERE (2026-07-17) — Sell-Out в рублях посчитан по ценовой политике
Раньше «Продажи, руб» были **null** у сетей, чьи отчёты содержат только штуки (Диалог, Фармаимпекс, Соц.аптека, Ригла…):
`dim_price` был пуст, `rub_est` считался только там, где рубли есть в отчёте (НеоФарм/еАптека). Руководитель добавил файл
**«Ценовая политика LIVS 1 января 2026»** (лист «Аптеки (ком)», ID 6835xxx + англ.наименования). Сделано:
- **`core.dim_product_price`** (новая таблица, добавлена и в `db/01_schema.sql`): product_id → price_vat (Цена с НДС =
  Прайс Базовый ПП = закупка = Sell-In по опр. руководителя) + rrc + approx. Наполняет **`app/load_price_policy.py`**
  (первое вхождение каждого ID в листе = каноничный прайс; ниже поклиентские блоки со скидками — игнор). Загружено
  **22 мастера** (21 прямой + IMMUN_PLUS = прокси по цене Иммун Систем, approx=TRUE).
- **`v_sellout`** и **`v_client_analysis`**: `rub_est = COALESCE(отчётные рубли, ROUND(qty*price_vat,2))::numeric(16,2)`
  (LEFT JOIN dim_product_price). НеоФарм/еАптека сохраняют реальные рубли, остальные считаются по прайсу.
- **Итог:** без рублей осталось **4.3% штук** (было ~74%): 21755 — «Не классифицировано» (нельзя оценить), +1017 шт =
  Фолиевая/Пренатал/Электролиты. Диалог теперь 22080 шт → 23.3 млн ₽.
- **СНЯТЫЕ С ПРОДАЖИ (2026-07-18):** руководитель: эти 3 товара (Фолиевая, Пренатал, Электролиты) больше НЕ продаём, но
  оставляем в истории. Поэтому их и нет в текущей прайс-политике (не пробел — сняты). Добавлен флаг **`core.dim_product.active`**
  (BOOLEAN, DEFAULT TRUE; в 01_schema.sql), три позиции = FALSE. Витрины НЕ фильтруем (остаются в истории). build_master.py
  флаг не сбрасывает (ON CONFLICT DO UPDATE SET name — active не трогает). Рубли им не нужны.
- ⚠️ Оговорка для сдачи: расчётные рубли — по **прайсу с НДС (list price)**, т.е. оценка сверху; реальные отпускные
  (НеоФарм ~692 ₽/шт) ниже прайса из-за скидок. Реальные рубли из отчётов не перезатёрты (COALESCE).
- Дашборды НЕ пересобирал — карточки читают вьюхи вживую, рубли появятся сами. Бэкап `backup/livs_bi.dump` обновлён.

## ⭐ RESUME HERE (2026-07-11) — где мы
**ВСЯ аналитика ТГ-списка (10/10) ГОТОВА. 12 дашбордов** в коллекции «LIVS BI» (см. ниже). Полный статус для сдачи —
`livs-bi/ОСТАЛОСЬ_перед_сдачей.md`. Дизайн/решения/ответы руководителя — глобальная память `livs-bi-replacement-design.md`.

**Дашборды (id):** 2 Продажи · 3 Остатки/закуп · 4 Sell-In · 5 Сверка SI/SO · 6 Валовая маржа(1С) · 36 Динамика ·
37 Клиенты/каналы · 38 Покрытие · 68 Матрица · 69 Доходность(Комм.прибыль) · 70 План-факт · 71 Прогноз.

**Данные:** продажи 524k шт, остатки 889k, закуп точек 253k, Sell-In(1С) 1.18млн шт/946млн ₽. + план-факт, доходность,
инвестиции, прогноз, матрица (вычисляемая), города точек (68% покрытие).

**ДЕПЛОЙ (демо, 2026-07-11):** Cloudflare quick-tunnel к Metabase (`docker run ... cloudflared`, protocol http2).
Публичные ссылки на 12 дашбордов включены (`app/mb_public.py`); view-only юзер — логин см. `CREDENTIALS.local.md` (вне git).
URL туннеля ЭФЕМЕРНЫЙ (меняется при рестарте) → перезапуск+печать ссылок: `scripts/start_tunnel.ps1`. Публичные
UUID СТАБИЛЬНЫ (в start_tunnel.ps1). Засыпание ПК отключено (`powercfg standby-timeout-ac 0`). Стабильно на дни =
боевой RF-сервер (DEPLOY.md).

**ПРАВКИ РУКОВОДИТЕЛЯ (2026-07-11) — ВСЕ 6 СДЕЛАНЫ**, см. `livs-bi/ПРАВКИ_руководителя.md`. Итог: новый дашборд
**id=100 «Клиент — разбор»** (v_client_analysis, db/A_client_analysis.sql, build_client_dashboard.py; фильтр Метрика
Sell-In/Sell-Out + Клиент + Год; по годам/месяцам ₽+шт, по SKU доли SI/SO + ТТ). «KPI» убрано из карточек. #4 покрыт id=70.
Теперь ДАШБОРДОВ 13. NB: v_client_analysis UNION Sell-Out(с tt) + Sell-In(1С, tt=NULL) — ТТ только для Sell-Out.

**БАГ СТОКА ИСПРАВЛЕН (2026-07-11):** руководитель нашёл — `v_stock_latest`/`v_stock_coverage` брали DISTINCT ON
(client,tt,sku) = тянули старые значения выпавших точек вперёд, завышали сток в 2.5x (по всем сетям: Вита +1492%,
Соц.аптека +157%, НеоФарм +34%…). Исправлено на «последний снимок КЛИЕНТА» (MAX(snapshot_date) per client). НеоФарм
теперь 8647шт/7.1млн₽ = бьётся с Sales Retail. См. AI_ERROR_LOG.
**«ПРОВЕРКА ДАННЫХ» id=132 (2026-07-11):** дашборд-страховка при самостоятельной загрузке. Витрина `v_dq_monthly`
(db/B_data_quality.sql, помесячные итоги+MoM), скрипт `build_dq_dashboard.py`. Ловит: резкие скачки MoM (±50%),
аномальные строки (qty>20000/<0), кто отстаёт по отчётам, неклассиф. SKU. Теперь ДАШБОРДОВ 14.

**ДАШБОРД id=2 ПЕРЕСОБРАН → «Продажи» (2026-07-11, правки руководителя):** был MBQL «Продажи и остатки», стал НАТИВНЫЙ
(`app/build_sales_dashboard.py`, перестраивает id=2 на месте — публ. ссылка сохранена). Раскладка: 3 KPI (шт/руб/ТТ) ·
продажи по годам руб+прирost% / шт+прирост% (bar, auto_split осей) · пивот год×месяц шт+Итого+Прирост% · всего шт +
по городам · SKU×месяц. Фильтры Клиент/Год(по умолч.2025)/Товар на v_sellout. Старые MBQL-карточки 40-44,46-49 осиротели
(не на дашборде). ВАЖНО: id=2 теперь нативный — fix_main_filters.py к нему больше НЕ применять (только id=3).

**🔜 СЛЕДУЮЩЕЕ:** отдать сайт руководителю (туннель эфемерный — start_tunnel.ps1). Ждём внешнее (матрицы/прайсы/каналы/RF-сервер).
Ждём внешнее: полные матрицы, поклиентские прайсы, финализация 55 «Не классифицировано», RF-сервер.
Отложено пользователем: авто-синк план-факта из файлов КАМов. Блокер данных: помесячный слой план-факта (файл квартальный).

## Глобальная цель
Заменить PowerBI: загрузить корпус отчётов 37 аптечных сетей (incoming/отчеты) в Postgres,
показать аналитику в Metabase (рус., светлая тема, мастер-товары). Периметр = ТГ-список (10 задач).

## СПРАВОЧНИК КОНТРАГЕНТ↔СЕТЬ + СВЕРКА (2026-06-30)
`core.dim_client.group_name` — группа (сеть/канал). `app/build_groups.py` (MAP: контрагент 1С→сеть, CAT:
маркетплейсы/дистрибьюторы). Применено 28 контрагентов (17 сетей + 11 каналов). Витрина `marts.v_reconcile`
(db/91_reconcile.sql): Sell-In vs Sell-Out по группе×месяцу + отношение SO/SI. Инсайт: SO/SI>1 (Фармаимпекс 3.0,
НеоФарм 2.3) = сеть докупает через дистрибьюторов; SO/SI<1 (Фармленд 0.2) = затарка/неполная отчётность.
15 сетей с обеими сторонами. Осталось: пользователь подтверждает справочник (28 сопоставлено, ~35 контрагентов
1С не привязаны — распределить по каналам/сетям). Дашборд сверки — можно сделать.

## 1С SELL-IN ЗАГРУЖЕН (2026-06-30)
`app/load_1c.py` — парсит сводный пивот 1С (`incoming/1С/1С.xlsx`): строки Менеджер→Покупатель(юрлицо)→
Номенклатура, столбцы месяц×[Количество,Выручка,Рентаб.,Себест.]. Отслеживает текущего Покупателя (юрлицо
по ООО/ЗАО/…), берёт строки-товары ЛИВС → `core.fact_sellin` (кол-во + выручка руб). Итог: 54 мес, 11571 строк,
63 контрагента, 90 товаров. Sell-In: 2022=112k шт/71.6М₽, 2023=250k/172М, 2024=340k/250М, 2025=346k/318М,
2026=133k/134М. Классифицирован в мастер-товары. Часть контрагентов = наши сети (НЕО-ФАРМ→НеоФарм,
ФАРМАЦЕВТ→Соц.аптека, ФАРМЛЕНД→Фармленд) + маркетплейсы (ИНТЕРНЕТ РЕШЕНИЯ=Ozon, ВАЙЛДБЕРРИЗ/РВБ=WB) +
дистрибьюторы (Катрен/БСС). ДАЛЬШЕ: (a) дашборд Sell-In (руб!), (b) справочник контрагент 1С↔сеть для сверки
Sell-In vs Sell-Out (Задача 1), (c) себестоимость из 1С → маржа/доходность (Задача 10). Себест./Рентаб. пока НЕ грузим (есть в файле).

## КОРПУС ЗАГРУЖЕН ПОЛНОСТЬЮ (2026-06-30)
Все 36 сетей, пустых 0, аномалий 0, дат<2020 нет. Продажи 524 455 / Остатки 889 262 / Закуп 253 168 шт, 2022-2026.
Хвост добит: Гармония (12/12, авто знает СумКол/СумКолОстКП; объём реально мал — 16 шт), ЛекОптТорг (28/28,
авто ловит «шт»/«Препарат»/SKU ID; акции skip). Приёмы: custom-обёртка `auto.adapt(f,p,CLIENT)` для мульти-фактовых;
auto понимает бренды livs/ливс/lvs и колонки-точки исключены из товара. Дальше: 1С Sell-In, деплой на RF-сервер,
задачи 8-10 (план/матрица/доходность), полировка дашбордов.

## Текущая задача
**ГОТОВО К ПОКАЗУ (2026-06-29).** Корпус авто-загружен, дашборд собран.
ВАЖНО: 686 файлов имели «расширение»-дату (`продажи_03.23` — Windows видел тип «.23»), конвейер их
не видел. Переименованы в .xlsx по сигнатуре (PK) → разблокирована глубина 2023–2025.
Итоги после: **Продажи 417 163 шт / Остатки 453 654 / Закуп 165 132; 25 сетей; аномалий нет.**
Глубина по годам (продажи): 2022=3707, 2023=57226, 2024=141210, 2025=174079, 2026=40941(неполн).
98%+ товаров в мастер-артикулах. GUI загрузки (Streamlit, :8501) с выбором клиента из списка и
заменой месяца. На будущее: новые файлы через GUI грузятся по содержимому (расширение не важно).
Дашборд id=2: 10 карточек (KPI Продажи/Сетей/Точек, Продажи по годам YoY, Динамика по месяцам,
по клиентам, топ мастер-товаров, по городам, остатки) + 3 фильтра (Клиент/Товар/Год).
Хвост (нужна доработка): Фарматека (пивот с подытогами), Ригла (широкий пивот + чужой бренд ABC),
Фармперспектива (битый xlsx без sharedStrings). Пустые папки: Алоэ/Двинафарм/ЛюбимаяАптека.

## Что готово
- Каркас: Docker (Postgres16 + Metabase + app3.11), схема staging/core/marts, мастер-товары (dim_product, 24), классификатор build_master.
- Батч-загрузка (batch.py + registry.py + parse_period), идемпотентность, auto-sku, xlrd.
- YAML-движок: алиасы колонок, tt_compose, tt_inn/city/chain, **header_detect** (авто-поиск строки шапки), фильтр строк-итогов.
- Metabase: рус + светлая тема, витрины переименованы (рус.), дашборд id=2 (5 MBQL-карточек, http://localhost:3000/dashboard/2).
- **ВОЛНА 1 ЗАГРУЖЕНО ЧИСТО: 11 клиентов, ~270 670 шт продаж, 17 576 точек, 2022–2026:**
  Фармаимпекс 118052, НеоФарм 63668, Аптека.ру 39974, Соц.аптека 27465, Планета 7992, еАптека 7221,
  Диалог 3018, Вита-Фарм(Томск) 2457, Гармония 435, ЛекОптТорг 277, Невис 111.
- Переходники: custom (dialog, neofarm, planeta, vita_tomsk, vita_samara[ОТКЛЮЧЁН]) + yaml (eapteka, apteka_ru, lekopttorg, sotsialnaya, nevis, farmaimpeks, garmonia).

## АВТО-ПЕРЕХОДНИК + ПОЛНЫЙ КОРПУС (2026-06-27, ключевое)
Построен `app/adapters/auto.py` — авто-разбор Excel БЕЗ ручного конфига (header_detect + словарь
синонимов колонок + авто-определение факта продажи/остаток/закуп + фильтр бренда ЛИВС + отсечение
итогов + исключение денежных колонок). batch использует auto для клиентов вне реестра; `--all-folders`
грузит весь корпус. **Прогон по всему корпусу: ~24 сети с продажами, 302 415 шт, 19 741 точка, БЕЗ аномалий.**
Это и есть автоматизация («новая сеть грузится без кода»). Нулевые (нужна доработка авто/спец-формат):
Фармакопейка(95ф), Фарматека, Мелодия СПБ, Ригла(чужой бренд+пивот), Фармперспектива(битый xlsx).
Пустые папки: Алоэ, Двинафарм, ЛюбимаяАптека.

## Добор фактов (2026-06-29)
Паттерн: custom-обёртка `app/adapters/custom/<c>.py` = `auto.adapt(file, period, CLIENT)` — авто-движок
(все листы/факты) под каноническим именем. Применено: Фармаимпекс (44/44: продажи 123k + остатки 12k),
Соц.аптека (58/58: продажи 39k + остатки 41k + закуп 16k). Итоги: продажи 434k / остатки 507k / закуп 193k.
ЛюбимаяАптека = 4 CSV в cp1251, по-чековый формат (sep=';') — нужен отдельный ридер CSV/cp1251.
Остаются пустыми 3: Ригла (широкий пивот+чужой бренд ABC), Фарматека (пивот с подытогами),
Фармперспектива (83 xlsx без sharedStrings — нужен спец-ридер). (b) форматные варианты (Гармония/ЛекОптТорг/
Мелодия МО/еАптека-2026) — малый объём, мессы.

## Фармперспектива + фикс битых xlsx (2026-06-30)
auto._fix_xlsx: чинит xlsx, где часть названа `xl/SharedStrings.xml` (др. регистр) — пересобирает архив
(openpyxl падал). Общий фикс. custom/farmperspektiva.py: парсит ЗАКУП-файлы (иерархия Поставщик/Товар|Кол-во,
имя товара в самой правой непустой ячейке; пропуск ООО/итогов; весь файл=ЛИВС, без бренд-фильтра) → закуп 99 строк/2492 шт/12 мес.
Остатки/продажи Фармперспективы — широкие пивоты, пока НЕ грузятся (нужен ещё адаптер, объём малый).

## ИТОГ КОРПУСА (2026-06-30, после хвоста)
**Все 36 сетей загружены, пустых нет, аномалий нет.** Продажи 523 914 / Остатки 889 055 / Закуп 251 774 шт.
По годам продажи: 2022=3836, 2023=108734, 2024=152538, 2025=216569, 2026=42237(неполн). Фикс бага:
`parse_period` — DD.MM.YYYY проверяется РАНЬШЕ MM.YY (иначе `_01.01.2023` → 2001; задело только Фармперспективу).
Остаток (мелочь, форматные варианты): Гармония (3 шт из 16ф — «СумКол»), ЛекОптТорг (277, старые форматы), РКМ (311).

## Хвост — добиваем (2026-06-30)
Готово: Фармперспектива (закуп+остатки+продажи, custom со spec-ридером битых xlsx + unpivot широкой матрицы),
Ригла (СТМ ЛИВС/ABC, custom unpivot товар×месяц), Фарматека (плоский, фикс бренда LVS), ЛюбимаяАптека
(CSV/cp1251 custom + batch видит .csv), Мелодия МО (фикс product-exclude). Важный фикс auto: из определения
ТОВАРА исключены колонки-точки (аптек/склад/юр/…) — Соц.аптека стала 72k (была 39k, мис-маппинг). Идёт
полный перезалив с исправленным auto. Остаток: Гармония (нестанд. «СумКол»), ЛекОптТорг (старые форматы 2/31),
еАптека-2026 — мелкие/мессовые форматные варианты.

## (устар.) Остаток хвоста (низкий ROI, после показа)
- Фармперспектива остатки/продажи (широкие пивоты), Ригла (пивот+чужой бренд), Фарматека (пивот с подытогами),
  ЛюбимаяАптека (4 CSV cp1251 по-чеково), форматные варианты (Гармония/ЛекОптТорг/Мелодия МО/еАптека-2026).
- Приём для мульти-фактовых/нестандартных клиентов: custom-обёртка `auto.adapt(file,period,CLIENT)`.

## Следующие шаги (TODO)
- [ ] Догрузить ещё клиентов группы A/B (Фармаимпекс 47ф, Гармония, ТвойДоктор, Фитофарм, Юнифарма, Эдельвейс...).
- [ ] Собрать/обновить дашборды в Metabase на загруженном (фильтры Год/Месяц/Клиент/Товар/Город, YoY, динамика) — для показа клиенту.
- [ ] Починить Вита (Самара) широкий формат 2026 → включить обратно.
- [ ] Варианты конфигов по «эрам» для ЛекОптТорг/Соц.аптеки (старые форматы).
- [ ] .xls-клиенты (Мелодия МО, Фармапрофит, Фиалка), битые xlsx (Фармленд/Фармперспектива).
- [ ] Спец: _ecom, ОСГ/сроки, «База клиентов» (обогащение ТТ ИНН/адресом).
- [ ] Открытые вопросы §9 плана (ОАС промо?, ecom канал?, ОСГ метрика?, Аптека.ру клиент?).
- [ ] 1С Sell-In; перенос на RF-сервер (DEPLOY.md).

## Дашборды Metabase (2026-06-29)
- **id=2 «Продажи»**: KPI(продажи/сети/точки) + по годам(YoY) + динамика по месяцам + по клиентам +
  топ мастер-товаров + по городам + **карта продаж по городам (pin, 57 городов)** + остатки. Фильтры: Клиент/Товар/Год.
- **Фильтры основных дашбордов пере-привязаны (2026-07-02)** через `app/fix_main_filters.py` (MBQL, self-heal
  по текущим field id). Продажи/Закуп: Клиент/Товар/Год — работают. Остатки (v_stock_latest): Клиент+Товар
  (Год к снимку не применяется). Был баг: остатки смотрели на поле v_stock вместо v_stock_latest — исправлено,
  в v_stock_latest добавлен мастер-`product`. Все фильтры проверены на движке дашкарт.
- **id=3 «Остатки и закуп»**: KPI(остатки/закуп) + остатки по клиентам + закуп по клиентам/месяцам/товарам. Фильтры: Клиент/Товар/Год.
- **id=5 «Сверка Sell-In / Sell-Out»** (2026-07-01): 3 KPI (Sell-In шт / Sell-Out шт / общий SO/SI) + bar
  Sell-In vs Sell-Out по сетям + таблица с коэффициентом + линия динамики по месяцам. Скрипт
  `app/build_recon_dashboard.py` (идемпотентный, нативный SQL, карточки 63-68). 15 сверяемых сетей.
  Инсайт: SO/SI>1 (Фармаимпекс 3.0, НеоФарм 2.3) = докупают через дистрибьюторов; SO/SI<1 (Фармленд 0.2) = затарка/неполн. отчётность.
- **id=6 «Доходность (Sell-In)»** (2026-07-01, задача 10): 3 KPI (Выручка/Прибыль/Рентаб.%) + bar Выручка↔Прибыль
  по годам + линия рентаб.% по месяцам + таблицы Топ сетей / Топ товаров по прибыли. Скрипт
  `app/build_profit_dashboard.py`, витрина `marts.v_profit` (db/92_profit.sql), карточки 69-75.
  Источник — 1С: `fact_sellin.cost_rub` (себестоимость, добавлена в схему + load_1c читает столбец j+3=Себест.).
  Прибыль = rub−cost_rub; рентаб.% = SUM(прибыль)/SUM(выручка). Итоги: выручка 946 млн, прибыль ~680 млн, ~72%.
  Рентабельность из 1С (столбец j+2) — это %, выводится, отдельно не хранится.
- **Фильтры на id=5 и id=6** (2026-07-02): field-filter'ы через `app/mb.py` (общие хелперы MB API).
  id=6 «Доходность»: фильтры Год/Сеть/Товар; id=5 «Сверка»: Год/Сеть (товара в v_reconcile нет).
  Паттерн: `WHERE {{year}} AND {{grp}} [AND {{prod}}]` (field-filter -> 1=1 если пусто), `has_field_values=list`
  для выпадашек. ВАЖНО: где используется field-filter, витрину НЕ алиасить (MB подставляет полное имя
  `marts.v_reconcile."Год"`) — линия сверки переписана через `IN (SELECT g FROM r)`. В API значение
  field-filter'а `=` передаётся МАССИВОМ (`[2025]`), UI шлёт массив сам. Проверено: фильтры считают верно.
- **id=36 «Динамика и прирост»** (2026-07-07, ответ руководителя Q1): продажи+закупки по месяцам (линия) +
  прирост MoM/QoQ/YoY + **YtD по годам (сравнимое окно)** + 3 KPI (Продажи/Закупки YtD, прирост YtD YoY).
  Витрина `marts.v_dynamics` (db/93_dynamics.sql: Sell-Out+Sell-In по сеть×товар×месяц через group_name).
  Скрипт `app/build_dynamics_dashboard.py` (карточки 89-96). Фильтры Сеть/Товар (Год НЕ ставим — сломает YoY).
  Оконные функции LAG (месяц со «спайном» generate_series для корректного YoY при пропусках). Прирост продаж
  по годам: 2024 +41%, 2025 +42%, 2026 неполный. NB: тестировать фильтры через python/requests (UTF-8),
  PowerShell Invoke-RestMethod шлёт кириллицу не в UTF-8 → пустой матч (артефакт теста, не бага).
- **id=37 «Клиенты и каналы»** (2026-07-07): признак `core.dim_client.channel_type` (Аптечная сеть/Дистрибьютор/
  Маркетплейс/Ритейл/Онлайн-аптека/Не классифицировано — правило в build_groups по суффиксу группы + sell-out).
  Витрина `marts.v_channel_summary` (db/94_channels.sql). Карточки 97-102, фильтр Канал. **Доля клиента в Sell-In
  = ответ на Q7 (зад.5)**. Инсайт: Маркетплейс 36% Sell-In, Дистрибьютор 23%, Аптечная сеть 20%. «Не классиф.» 33 —
  финализирует руководитель.
- **id=38 «Остатки: покрытие»** (2026-07-07): «на сколько хватит» = остаток / средние продажи (посл. 3 мес).
  Витрина `marts.v_stock_coverage` (db/95_coverage.sql, грейн сеть×товар). Карточки 103-108, фильтры Сеть/Товар.
  Ловит риск дефицита (сортировка по возрастанию месяцев). Дрилл по товару и сети.
- Карта: справочник `core.dim_city_geo` (57 городов, координаты) + витрина `marts.v_city_sales`
  (нормализация города из v_sellout, pin-карта по native-SQL; фильтры дашборда на native-карту НЕ влияют — это ок).
- «Неклассифицированные» товары: было 16 → **0** (правила build_master: «желез», латинская C, B+вит С,
  «для детей с 3 лет»; удалены 18 строк-итогов; melodiya_spb фильтрует итоги).

## GUI загрузки (2026-06-27)
`app/ui.py` (Streamlit) — окно загрузки отчётов: перетащить Excel -> авто-переходник разбирает ->
предпросмотр + проверка аномалий -> «Загрузить в базу» (сразу видно в Metabase) и «Скачать единый
шаблон (Excel)». Сервис `ui` в docker-compose, порт 127.0.0.1:8501. Запуск: `docker compose up -d ui`.
Это и есть «программа с GUI» из видения пользователя. «Загрузка на сайт» = кнопка -> БД -> Metabase.

## ОБОГАЩЕНИЕ ГОРОДОВ ТОЧЕК (2026-07-10)
`app/enrich_city.py`: заполняет `dim_tt.city` из имени/адреса точки (словарь ~130 городов whole-word + шаблон
«<Город> г,» для адресов НеоФарма). Только пустые, не перетирает. Итог: продажи с городом 174k→343k, null 350k→181k;
НеоФарм 142k→4.8k (Москва 104k). Запускать ПОСЛЕ batch-загрузок (карта «Продажи по городам» id=2 и v_city_sales
подхватывают dt.city сами). ОСТАТОК null: Соц.аптека 72k (имя пустое, код «Бг-»), Ригла 44k, Вита Самара 19k,
Диалог 14k — нужны файлы «База клиентов» (incoming/*/База клиентов/, батч их пропускает; Фаза 2).
ФАЗА 2 (2026-07-10): `app/load_baza.py` — из «База клиентов»: Соц.аптека (префикс кода→город из адреса) + Диалог
(норм.код→Город). Итог: города 33%→68% (358k из 524k), null 166k. Фаза 2 дала мало (Соц.аптека 20, Диалог 54) —
файлы плохо стыкуются: файл Соц.аптеки только по Белгороду, у Виты коды мусорные, у Риглы/еАптеки ТОЧЕК НЕТ вообще
(продажи на уровне клиента, tt=null, ~53k — география невозможна структурно). Дальше упор в пределы данных.

## ДОРАБОТКИ (2026-07-11)
- **Прогноз**: `build_forecast.py` — последние 2 мес (лаг отчётов) теперь идут ПРОГНОЗОМ, не битым фактом
  (SKIP_RECENT=2); уровень run-rate по всей истории (самокоррекция). Факт до посл. полного мес, прогноз дальше.
- **Косметика городов**: `v_sellout.city` = `COALESCE(NULLIF(dt.city,''),'(город не определён)')` → на карточке «Продажи
  по городам» вместо null. Карта (v_city_sales) не затронута (JOIN к гео-справочнику отсеивает плейсхолдер).
- **Сверка план-факт↔1С** (`app/check_pf_1c.py`): Sell-In шт Q1 2026, матч по токенам имени. РЕЗУЛЬТАТ: где клиент
  есть в 1С — план-факт «факт» = 1С ТОЧЬ-В-ТОЧЬ (+0%: НеоФарм/Вита/Фармаимпекс/Диалог/Невис/Медэкспорт/…). Значит
  их факт берётся из 1С, ДАННЫМ МОЖНО ДОВЕРЯТЬ (сомнение руководителя снято). «Нет в 1С» = маркетплейсы/ритейл
  (иной канал учёта) + несматченные имена. ⚠️ ОСТАЛОСЬ (блокер данных): помесячный слой план-факта — файл ПЛАН ФАКТ
  КВАРТАЛЬНЫЙ, чистого помесячного 2026-плана по клиентам нет (2027 SOP помесячный есть; 2026 — нужен файл).

## ПРОГНОЗ (2026-07-10, зад.7) — ГОТОВО
Дашборд id=71 «Прогноз (продажи и закупки)» (коллекция LIVS BI). Таблица `core.fact_forecast` + витрина
`marts.v_forecast` (db/99_forecast.sql), скрипт `app/build_forecast.py` (метод: run-rate = ср. уровень 12 мес ×
сезонный индекс месяца; TRUNCATE+пересчёт; ИТОГО + сети с ≥12 мес истории; горизонт до 2027-12). Метрики Продажи
(fact_sellout) + Закупки (fact_sellin). kind=факт|прогноз. Фильтры Метрика(по умолч.Продажи)/Сеть(по умолч.ИТОГО).
Прогноз продажи 78k(ост.2026)+145k(2027), закупки 182k+317k. NB: прогноз местами выше последних фактов — последние
месяцы НЕПОЛНЫЕ (лаг отчётов), run-rate берёт средний уровень. Доработка: взвесить/отсечь неполные месяцы.

## ПЛАН-ФАКТ (2026-07-10, зад.9) — ГОТОВО (квартальный)
Дашборд id=70 «План-факт (Sell-In / Sell-Out)» (коллекция LIVS BI). Таблица `core.fact_plan_fact` + витрина
`marts.v_plan_fact` (db/98_plan_fact.sql), загрузчик `app/load_plan_fact.py` (листы «Ритейл 2026»+«E-com 2026»
из ПЛАН ФАКТ 2026: клиент colB, показатель colC, кварталы 1Q-4Q + год; план col6/10/14/18/22, факт-прогноз
col7/11/15/19/23; Q1=факт, Q2-4=прогноз). 659 строк, метрики Sell In/Out руб+шт. Фильтры Показатель(по умолч.
Sell In шт)/Клиент/Канал. Выполнение% = факт/план. Sell-In шт 2026: план 432k/факт 415k. NB: квартальный (помесячный
слой и сверка с 1С — доработка; руководитель не уверена, что план бьётся с 1С). Данные ЕЁ (самосогласованно).

## ДОХОДНОСТЬ ПО ФОРМУЛЕ (2026-07-10, зад.10) — ГОТОВО
Дашборд id=69 «Доходность клиента (Коммерческая прибыль)» (коллекция LIVS BI). Таблица `core.fact_client_econ` +
витрина `marts.v_dohodnost` (db/97_dohodnost.sql), загрузчик `app/load_dohodnost.py` (листы «свод доходность» +
«свод инвестиции» из Sales Retail, мёрж по нормализованному имени клиента: 28 клиентов, 27 с инвестициями).
Формула: Комм.прибыль = Доходность% × (SI − Бюджет). Итоги сошлись с файлом: SI план 187.7М, Бюджет 24.6М,
ср. доходность 56.9%, комм.прибыль 92.9М. Фильтры Канал/КАМ. Старый id=6 переименован в «Валовая маржа (Sell-In 1С,
факт)» — это ДРУГАЯ метрика (валовая маржа из 1С ~72%), не путать с доходностью (Коммерческая прибыль плана).
NB: доходность% берём готовую из файла (учитывает Себест ~250 ₽/шт); наш fact_sellin.cost_rub — для валовой маржи.

## МАТРИЦА — ВЫЧИСЛЯЕМАЯ ИЗ ОСТАТКОВ (2026-07-10, зад.8, Вариант B)
Дашборд id=68 (коллекция LIVS BI). Витрина `marts.v_matrix` — **ВЫЧИСЛЯЕМАЯ** из `core.fact_stock` (db/96_matrix.sql,
таблицы/загрузчика НЕТ — обновляется сама при загрузке остатков). Соблюдение = ТТ со стоком товара / все активные ТТ
сети в снимке; по МАСТЕР-товару (дубли SKU схлопываются). Работает для 18 сетей с поточечными остатками; фильтр Сеть
(по умолчанию Диалог) + Товар. Валидация: апр авторизовано=88 (как в файле руководителя), ср.соблюдение апр 46.7% vs
её 48.1% — сходится. Отличия по товарам с дублями SKU (D3: наш мастер считает оба написания) — принято (Вариант B).
Диалог: соблюдение падало 95%(2022)→44%(май2026). Скрипт `app/build_matrix_dashboard.py`. НЮАНС: «авторизовано» =
все активные ТТ (прокси); настоящие матрицы (какие SKU где положены) подставим, когда руководитель даст.

## VIEW-ONLY ПОЛЬЗОВАТЕЛЬ (2026-07-09)
`app/mb_viewer.py`: **view-only юзер** (логин см. `CREDENTIALS.local.md`, не админ). Понижена группа «All Users»: коллекция
LIVS BI (5) = read, данные db2 create-queries=no (view-data остаётся, download full). Проверено: видит 8 дашбордов,
карточки рендерятся, редактирование → 403. Админ (nikitapro…, группа Administrators) не затронут — union прав, правит всё.
Давать руководителю ЭТОТ логин, не админский. Публичные ссылки (без логина) — тоже работают.

## ДЕМО-ДЕПЛОЙ ЧЕРЕЗ ТУННЕЛЬ (2026-07-09)
Для показа руководителю→начальству: Cloudflare quick-tunnel к Metabase (данные остаются на ПК в РФ → 152-ФЗ ок).
Контейнер `livs_tunnel` (cloudflare/cloudflared, сеть livs-bi_default, `--protocol http2` — QUIC/UDP на сети юзера
заблокирован, отсюда были 530). Публичные ссылки (без логина) включены: `enable-public-sharing`, скрипт
`app/mb_public.py` (UUID дашбордов СТАБИЛЬНЫ). Перезапуск + печать ссылок: `scripts/start_tunnel.ps1`.
ВАЖНО: quick-tunnel URL МЕНЯЕТСЯ при перезапуске; ПК+Docker+туннель должны быть включены, пока смотрят.
Стабильный URL = named tunnel (нужен аккаунт Cloudflare + домен) или боевой RF-сервер (DEPLOY.md) — апгрейд на потом.
UUID: Доходность 3101d253… Динамика 20a75a62… Сверка b527fb14… Каналы 2754bf0a… Покрытие eb72cc73…
Продажи 8312a73e… Остатки/закуп c38ad802… Sell-In 64db3fdf…

## УБОРКА/ХВОСТЫ (2026-07-07)
Health-check всех 9 дашбордов — 0 сломанных карточек (`app/mb_maint.py`, только чтение). Сэмпл-дашборд
«E-commerce Insights» (id=1) в архиве, Sample Database уже удалена (осталась только LIVS id=2). Создана коллекция
Metabase **«LIVS BI» (id=5)** — все 8 наших дашбордов (2,3,4,5,6,36,37,38) перенесены туда. Добавлена утилита
`app/build_all_dashboards.py` (пересобрать все скриптовые дашборды + fix_main_filters одной командой, идемпотентно).
AI_ARCHITECTURE.md обновлён под текущее состояние (1С, каналы, витрины 90-95, дашборд-скрипты). **Дамп БД обновлён**
(`backup/livs_bi.dump`, теперь с channel_type и новыми витринами). Временных `_*.py` не осталось.

## ПЕРЕНОС НА ДРУГОЙ ПК (2026-07-06)
См. `MIGRATION.md`. Подготовлено: `backup/livs_bi.dump` (pg_dump -Fc, 147 МБ, вся БД), `metabase-data/`
(H2 Metabase со всеми дашбордами — вынесена из контейнера). **Metabase теперь ПЕРСИСТЕНТЕН**: в
docker-compose добавлен том `./metabase-data:/metabase.db` (раньше дашборды жили только в контейнере — риск
потери при пересоздании; исправлено). Схема в db/*.sql приведена к факту (`dim_client.group_name`,
`fact_sellin.cost_rub`). Восстановление на ноуте: up db → pg_restore --clean → up остальное; контекст Claude
(`~/.claude/projects/d--ToLivs/`: memory + транскрипты) копировать вручную, запускать из того же пути D:\ToLivs.
Проверено: тестовый Metabase на смонтированной H2 показал все 6 дашбордов. NB: порт 3000 бывает занят авто-форвардом VS Code.

## Логины/доступы
- GUI загрузки: http://localhost:8501
- Metabase: http://localhost:3000 — логин админа см. `CREDENTIALS.local.md` (СМЕНИТЬ для прода). DB id=2 (host `db`, db `livs_bi`).
- Память проекта (долгая): этот файл + AI_ARCHITECTURE.md + AI_ERROR_LOG.md. Глобальная авто-память: `~/.claude/.../memory/livs-bi-replacement-design.md`.
